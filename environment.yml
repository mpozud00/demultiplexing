# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: mpozud00-demultiplexing
channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python=3.6.10
  - fastqc=0.11.8
  - bioconda::cutadapt=3.0
